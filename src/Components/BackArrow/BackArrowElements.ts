import styled from 'styled-components'

export const Arrow = styled.div`
    position: absolute;
    top: 20px;
    left: 20px;
    transition: all 100ms ease-in;
    cursor: pointer;
    :hover {
        scale: 1.05;
    }
`
